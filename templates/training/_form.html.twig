<div class="d-inline">
    {{ form_start(form) }}
</div>
        {{ form_widget(form) }}
        {{ form_row(form.brochure) }}
        <a type="button" class="btn btn-outline-success" href="{{ path('admintraining_index') }}">Terug</a>
        <button class="btn btn-outline-warning d-inline">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}


{% if app.request.get('_route') != 'admintraining_new' %}
        {{ include('training/_delete_form.html.twig') }}
{% endif %}

