{{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_row(form.brochure) }}
    <a type="button" class="btn btn-outline-success" href="{{ path('training_index') }}">Terug</a>
    <button class="btn btn-outline-warning d-inline">{{ button_label|default('Save') }}</button>
{% if app.request.get('_route') != 'training_new' %}
    {{ include('training/_delete_form.html.twig') }}
{% endif %}
{{ form_end(form) }}
