{{ form_start(form) }}
    {{ form_widget(form) }}
    <a type="button" class="btn btn-outline-success" href="{{ path('lesson_index') }}">terug</a>
    <button class="btn btn-outline-warning ">{{ button_label|default('Save') }}</button>
    {% if app.request.get('_route') != 'lesson_new' %}
        {{ include('user/_delete_form.html.twig') }}
    {% endif %}
{{ form_end(form) }}
