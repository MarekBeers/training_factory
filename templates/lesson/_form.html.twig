<div class="d-inline">
    {{ form_start(form) }}
</div>
    {{ form_widget(form) }}
    <a type="button" class="btn btn-outline-success" href="{{ path('instructeurlesson_index') }}">terug</a>
    <button class="btn btn-outline-warning ">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
    {% if app.request.get('_route') != 'instructeurlesson_new' %}
        {{ include('user/_delete_form.html.twig') }}
    {% endif %}
